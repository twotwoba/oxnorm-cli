# Support multiple platforms for native dependencies (oxlint, oxfmt)
# This ensures lockfile includes bindings for all platforms
supported-architectures[] = osx
supported-architectures[] = linux
supported-architectures[] = win32
supported-architectures[] = x64
supported-architectures[] = arm64

# Use hoisted node_modules for better native module compatibility
shamefully-hoist = true
